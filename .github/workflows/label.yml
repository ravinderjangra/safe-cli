name: Labeler
on:
  pull_request:
    branches: Refactor-GHA
  push:
    branches: Refactor-GHA

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/labeler@v2
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
